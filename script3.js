"use strict";
/*class Pelicula{
    public titulo?: string;
    public director?: string;
    public anio?: number;
    public calificacion?: number;
    public protagonistas?: string[];

    proyectarEnCine(){
        console.log(`${this.titulo} se proyectara en el cine`)
    }

    constructor(titulo?: string,  protagonistas?: string[]) {
        this.titulo = titulo;
        this.protagonistas = protagonistas;
    }
}
const peli = new Pelicula();
//pelicula.proyectarEnCine();

const pelicula = new Pelicula('Barbie',['Barbie','Margot Robbie']);
console.log(pelicula);
const pelicula2 = new Pelicula('Oppenhimer',['Oppenheimer','Cillian Murphy']);
console.log(pelicula2);

pelicula.proyectarEnCine();
console.log(pelicula2);

*/
/*class Sorteo<T>{
    private ticket?: T;

    constructor(private nombre: string) {}

    setTicket(ticket: T){
        this.ticket = ticket;
    }

    getTicker(){
        return this.ticket;
    }
    
    public sortear(): string{
        return `Para ${this.nombre} el ticket es ${this.ticket}`
    }
}

let sorteo = new Sorteo<string>("Sergio");
sorteo.setTicket('A7');
console.log(sorteo.sortear());*/ 
